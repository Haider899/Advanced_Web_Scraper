# 🌐 Universal Web Scraper Pro

A powerful universal web scraper that works on ANY website. Features interactive menus, multiple export formats, and intelligent content extraction.

## 🚀 Quick Start

### 📥 Installation

#### Windows
```cmd
# Method 1: Using pip
pip install requests beautifulsoup4 pandas openpyxl lxml

# Method 2: Using requirements.txt (if downloaded)
pip install -r requirements.txt

---------
For Windows Users:

1. Install Python from python.org (check "Add to PATH")
2. Open Command Prompt in the project folder
3. Run: pip install -r requirements.txt
4. Run: python advanced_scraper.py

=====================================
=====================================
####Kali

# Install Python and pip (if not already installed)
sudo apt update
sudo apt install python3 python3-pip

# Install dependencies
pip3 install requests beautifulsoup4 pandas openpyxl lxml

# Or using requirements.txt
pip3 install -r requirements.txt
--------
For Kali
1. sudo apt install python3 python3-pip
2. pip3 install -r requirements.txt  
3. python3 advanced_scraper.py

=======================
=======================
macOS
bash
# Install Python (if not already installed)
brew install python

# Install dependencies
pip3 install requests beautifulsoup4 pandas openpyxl lxml

# Or using requirements.txt
pip3 install -r requirements.txt
---------
For macOS Users:
text
1. brew install python
2. pip3 install -r requirements.txt
3. python3 advanced_scraper.py


=========================
After running python advanced_scraper.py, you'll see:

text
🌐==================================================🌐
           UNIVERSAL WEB SCRAPER PRO
🌐==================================================🌐

📋 MAIN MENU - Choose an option:
1. 🎯 Scrape Single Custom URL
2. 🕷️  Crawl Entire Website
3. 📝 Scrape Multiple Custom URLs
4. ⚙️  Settings & Configuration
5. 🧪 Test on Popular Websites
6. ❌ Exit

Enter your choice (1-6):
